// export const sortStocks = (stocks, column, order) => {
//     // Sort stocks by the selected column and order
//     return [...stocks].sort((a, b) => {
//       if (order === 'asc') return a[column] - b[column];
//       else return b[column] - a[column];
//     });
//   };
  